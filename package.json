{
  "name": "vue-parcel-profile",
  "version": "1.0.0",
  "description": "A simple Vue app with a profile component, built with Parcel.",
  "source": "index.html",
  "scripts": {
    "start": "parcel --public-url / -p 8080",
    "build": "parcel build --public-url /"
  },
  "devDependencies": {
    "@parcel/optimizer-esbuild": "^2.12.0",
    "@parcel/transformer-vue": "^2.12.0",
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "vue": "^3.4.27"
  }
}
